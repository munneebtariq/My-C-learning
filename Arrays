#include<stdio.h>
int main(){
    int a;
    printf("Enter index:");
    scanf("%d",&a);
    int arr[a];
    printf("Enter the elements of an array:");
    for(int i=0;i<a;i++){
    scanf("%d",&arr[i]);
    }
    int n;
    printf("Enter index:");
    scanf("%d",&n);
    for(int i=a;i>=n;i--){
    arr[i]=arr[i-1];
    }
    arr[n-1]=6;
    a++;
    printf("New array:");
    for(int i=0;i<a;i++){
    printf("%d",arr[i]);
    }
    return 0;
}
#include<stdio.h>
int main(){
    int a;
    printf("Enter index:");
    scanf("%d",&a);
    int arr[a];
    printf("Enter the elements of an array:");
    for(int i=0;i<a;i++){
    scanf("%d",&arr[i]);
    }
    for(int i=a;i>=0;i--){
    arr[i]=arr[i-1];
    }
    arr[0]=6;
    a++;
    for(int i=0;i>a;i++){
    arr[i]=arr[i-1];
    }
    arr[a]=8;
    a++;
    printf("New array:");
    for(int i=0;i<a;i++){
    printf("%d",arr[i]);
    }
    return 0;
}
#include<stdio.h>
int main(){
    int a;
    printf("Enter index:");
    scanf("%d",&a);
    int arr[a];
    printf("Enter the elements of an array:");
    for(int i=0;i<a;i++){
    scanf("%d",&arr[i]);
    }
    for(int i=0;i>a;i++){
    arr[i]=arr[i-1];
    }
    arr[a]=8;
    a++;
    printf("New array:");
    for(int i=0;i<a;i++){
    printf("%d",arr[i]);
    }
    return 0;
}
#include<stdio.h>
int main(){
    int a;
    printf("Enter index:");
    scanf("%d",&a);
    int arr[a];
    printf("Enter the elements of an array:");
    for(int i=0;i<a;i++){
    scanf("%d",&arr[i]);
    }
    for(int i=0;i>a;i++){
    arr[i]=arr[i-1];
    }
    arr[a]=8;
    a++;
    printf("New array:");
    for(int i=0;i<a;i++){
    printf("%d",arr[i]);
    }
    return 0;
}
